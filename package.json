{
  "name": "preprovision-monorepo",
  "private": true,
  "workspaces": [
    "Backend/*",
    "Frontend/*",
    "App/*",
    "Desktop/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
